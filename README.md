# Build command for crave free build server
POCO F4 (munch) SD870, 6,8,12GB 128,256GB

```
crave run --no-patch -- "curl https://raw.githubusercontent.com/Pry28/Crave/refs/heads/16/build.sh | bash"
```
